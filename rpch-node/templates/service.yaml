apiVersion: v1
kind: Service
metadata:
  name: {{ include "rpch-node.fullname" . }}
  labels:
    {{- include "rpch-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rpch-node.selectorLabels" . | nindent 4 }}
