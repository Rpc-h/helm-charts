apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rpch-node.fullname" . }}-test-connection"
  labels:
    {{- include "rpch-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rpch-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
